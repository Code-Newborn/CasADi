{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run MSVC Debug",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Debug/casadi.exe",
            "cwd": "${workspaceFolder}/Debug",
            "preLaunchTask": "Build MSVC Debug",
            "console": "integratedTerminal",
            "environment": [
                {
                    "name": "PATH",
                    "value": "${config:vsImguiBin};${config:vsGlfwBin};${config:vsImplotBin};${config:vsCasadiBin}/Debug;${config:vsSystem32};${env:PATH}"
                }
            ]
        },
        {
            "name": "Run MSVC Release",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Release/casadi.exe",
            "cwd": "${workspaceFolder}/Release",
            "preLaunchTask": "Build MSVC Release",
            "console": "integratedTerminal",
            "environment": [
                {
                    "name": "PATH",
                    "value": "${config:vsImguiBin};${config:vsGlfwBin};${config:vsImplotBin};${config:vsCasadiBin}/Release;${config:vsSystem32};${env:PATH}"
                }
            ]
        }
    ]
}